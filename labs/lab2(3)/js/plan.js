//set up global variable for circle size

//set up canvas

//set up circle in middle of canvas along with background and any other styling choices
    //connect variable to circle

//If the circle is less than 200, then increase the circle diameter by 1.
    //Otherwise reset the circle to 1. (If the circle reaches 200 reset the circle diameter to 1.)